<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:material_inout_desktop_v2.ViewModels"
             xmlns:entities="clr-namespace:material_inout_desktop_v2.Entities"
             x:Class="material_inout_desktop_v2.MainPage"
             x:DataType="vm:MainViewModel"
             >

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            
            <Label
                Text="{Binding Title}"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Entry x:Name="barCodeInput" TextChanged="OnBarCodeTextChanged" />

            <CollectionView HeightRequest="200" ItemsSource="{Binding Articles}">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="vm:ArticleViewModel">
                        <Grid Padding="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Label Text="{Binding Article.EAN}" />
                            <Label Grid.Column="1" Text="{Binding Article.Label}" />
                            <Button Grid.Column="2"
                                Text="Enlever"
                                Command="{Binding RemoveCommand}"
                                CommandParameter="{Binding .}"
                                Padding="1,1" />
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
            
            <Label Text="Nom du responsable:" />

            <Entry Text="{Binding PersonResponsible}" />

            <Button
                Text="Créer bon de sortie"
                Command="{Binding CreateVoucherCommand}"
                 />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
