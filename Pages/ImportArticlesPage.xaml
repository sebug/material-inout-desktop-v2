<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:material_inout_desktop_v2.ViewModels"
             x:Class="material_inout_desktop_v2.Pages.ImportArticlesPage"
             x:DataType="vm:ImportArticlesViewModel"
             >

    <ScrollView>
        <VerticalStackLayout
            Spacing="25"
            Padding="30,0"
            VerticalOptions="Center">

            <Label
                Text="Import Articles"
                SemanticProperties.HeadingLevel="Level1"
                FontSize="32"
                HorizontalOptions="Center" />

            <Button Text="Importer fichier d'articles"
                Command="{Binding ImportCommand}" />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>